<!--pages/order/order.wxml-->
<head text="预约"></head>
<view class="container" style="padding-top:{{pageTopHeight}}px">
 <view wx:for="{{list}}" wx:key="index" data-id="{{item.id}}" class="list">
  <view class="goods-item">
    <text class="iconfont icon_sell item-sell"></text>
    <image src="{{item.src}}" class="item-picture" bindtap="toDetails" style="border-radius:4px"></image>
    <view class="titles">
     <view class="title">{{item.title}}</view>
     <view class="subtitle">{{item.subtitile}}</view>
    </view>
    <view class="item-bottom">
      <view class="price">
        <text class="sell-price">￥{{item.price}} </text><text style="font-size:12px;color:#de4b4e">/次</text>
        <text class="old-price">￥{{item.oldPrice}}</text>
      </view>
      <view class="order-botton" bindtap="toCheck">立即预约</view>
    </view>
  </view>
 </view>
</view>
